{
    "config": {
        "run": {
            "mode": "docker",
            "exec": "docker-compose -f ./docker-compose.yml run --rm -T laravel.test",
            "path": "./vendor/bin/captainhook"
        }
    },
    "pre-commit": {
        "enabled": true,
        "actions": [
            {
                "action": "php artisan test --parallel"
            }
        ]
    }
}
